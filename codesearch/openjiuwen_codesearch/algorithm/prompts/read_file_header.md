File contents (numbered lines). If this window contains code that must be changed/read for the fix, call add_context on the tightest enclosing function/method next — reading alone does not count as retrieved context.
Path: {path}
Lines: {start}-{end} of {total}
