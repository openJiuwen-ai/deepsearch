# <特性名>

## 维护范围

<本文档覆盖哪些模块/行为；不覆盖什么（并给出指向）。>

## 功能目的

<一两句：这个特性解决什么问题、为什么这样设计。>

## 核心流程

<主链路的步骤/图；分支与循环的走向。>

## 可见行为

<对使用者/运维可观察的行为清单：默认值、终止条件、隔离保证、
性能/资源特征等。行为变更的 PR 必须同步更新本节。>

## 数据契约与依赖

<输入输出模型、session/state 携带的字段、对 base/外部服务的依赖。>

## 边界与错误处理

<失败路径、降级行为、异常收窄策略、超时与重试。>

## 测试与验证

<覆盖本特性的测试文件清单（unit/integration/e2e），行为契约由哪些用例锁定。>

## 关键代码路径

| 文件 | 内容 |
|---|---|
| `<path>` | <职责> |

## 已知限制与待办 / 相关文档

<边界、未覆盖场景、后续计划；关联的 feature 文档链接。>
