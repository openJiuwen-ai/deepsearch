# 特性名称

## 维护范围

说明本文档覆盖的能力边界，以及不覆盖的内容。
如果这是总览文档，列出子文档链接。

## 功能目的

说明这个特性解决什么问题，面向哪些用户、调用方或内部流程。

## 可见行为

说明用户、SDK 调用方、服务端 API 调用方或前端能观察到的行为。
只写行为，不写实现细节。

## 关键代码路径

列出主要入口文件、核心实现文件、相关 Prompt 和主要测试文件。
不要列完整调用链、私有方法清单或逐行解释代码；需要说明的关键步骤放到
“核心流程”中。

## 核心流程

用简短步骤说明主流程。
跨模块特性可以写数据如何从入口流到结果。

## 数据契约与依赖

说明关键输入、输出、状态字段、配置项、API schema、Prompt 输入变量、输出格式等契约。
不要复制 prompt 全文。

## 边界与错误处理

说明重要边界条件、异常码、fallback、资源清理、权限或路径安全要求。

## 测试与验证

列出推荐测试命令、相关测试文件、哪些场景必须覆盖。

## 相关文档

链接用户文档、开发指南、API 文档、设计文档或相关 feature 文档。
